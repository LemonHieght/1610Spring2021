using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActiveState : MonoBehaviour
{
    // public GameObject gameObject;
    
    // Start is called before the first frame update
    void Start()
    {
        //I know this wont work but I just wanted to make on script instead of two
        // gameObject.SetActive(false);
        // Debug.Log(("Active Self: " + gameObject.activeSelf));
        // Debug.Log("Activein Hierarchy: " + gameObject.activeInHierarchy);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
